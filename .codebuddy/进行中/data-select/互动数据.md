
# 新增上报-抽取数据
```sql
select *
from pcg_atta_public_tdbank::t_atta_v1_0e600066510
where tdbank_imp_date=2026031206  -- 最新分区
and uri = "/dmComment/create"   -- 通过uri来区分上报类型，每一个新上报都有唯一的一个uri
and code=0

```